Open the config file in an editor

Usage:
  config edit [options]
  config edit --help | -h

Options:
  --file, -f PATH
    Path to the config file

Environment:
  EDITOR
    Editor command to run. Defaults to vi.

Examples:
  config edit -f path/to/config.toml
  config edit
