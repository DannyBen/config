Generate shell completion scripts

Usage:
  config completion SHELL
  config completion --help | -h

Parameters:
  SHELL
    Shell to generate completions for: bash, zsh, fish

Examples:
  config completion bash
  config completion zsh
  config completion fish
